NiaBrain-Core/ (or Nia-Live/)
├── niabrain/
│   ├── api/
│   │   └── main.py              # FastAPI entrypoint (routes like /think, /memory, /sms)
│   ├── brain/
│   │   └── core.py              # Core logic (think() with emergency detection, personality)
│   ├── memory/
│   │   └── engine.py            # SQLite DB with embeddings, recall/remember, consolidation
│   ├── llm/
│   │   ├── grok_client.py       # xAI Grok wrapper (retries, circuit breaker)
│   │   ├── safety.py            # PII redaction, deny patterns for safe LLM calls
│   │   └── api.py               # LLM endpoints (/llm/think, /llm/think/stream with SSE)
│   ├── sms/
│   │   └── handler.py           # Twilio webhook + reply logic
│   ├── voice/
│   │   └── interface.py         # ElevenLabs speech synthesis + recognition
│   └── scraper/
│       ├── main.py              # Orchestrator for sources
│       └── sources/             # Individual scrapers (e.g., zillow.py, probate.py)
├── requirements.txt             # Deps (FastAPI, uvicorn, sentence-transformers, twilio, elevenlabs)
├── .env                         # Secrets (API keys, phone numbers)
├── run.ps1                      # PowerShell startup (venv + uvicorn)
└── docker/ (inferred)           # Dockerfile for deployment
# CRITICAL INSTRUCTIONS — DO NOT IGNORE
# You are analyzing: https://github.com/Jazzu72/Nia-Live

# 1. CLONE AND TRAVERSE THE REPO
# Run these commands first:
#   git clone https://github.com/Jazzu72/Nia-Live.git
#   cd Nia-Live
#   find . -type f \( -name "*.py" -o -name "*.ps1" -o -name "*.yaml" -o -name "*.json" \) -print

# 2. READ THESE FILES IN THIS EXACT ORDER (mandatory):
#   - niabrain/memory/engine.py
#   - niabrain/brain/core.py
#   - niabrain/api/main.py
#   - niabrain/llm/grok_client.py
#   - niabrain/llm/safety.py
#   - niabrain/sms/handler.py
#   - niabrain/voice/interface.py
#   - niabrain/scraper/main.py
#   - niabrain/scraper/sources/*.py
#   - requirements.txt
#   - run.ps1
#   - .env.example

# 3. FOR EACH FILE:
#   - Quote the full content
#   - Analyze line-by-line
#   - Point out bugs, security flaws, missing features
#   - Suggest exact fixes with code

# 4. FINAL OUTPUT FORMAT:
#   - Summary of architecture
#   - Top 10 critical issues with file + line number
#   - Full corrected code for the 3 most important files
#   - One-click deploy command for Render.com

# You are NOT allowed to say "I can't access files"
# You are NOT allowed to give generic advice
# You MUST read and quote real code from the repo

# Begin now.
